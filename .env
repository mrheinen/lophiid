# Docker Compose Environment Variables
# This file sets default values for user namespace mapping

# Set these to your current user to avoid permission issues
DOCKER_UID=1000
DOCKER_GID=1000

# Override with your actual UID/GID by running:
# export DOCKER_UID=$(id -u) DOCKER_GID=$(id -g)
# before running docker compose commands