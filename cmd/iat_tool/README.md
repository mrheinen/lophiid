This is a helper script to test the IAT analysis logic. Run it with a request ID
and see what the IAT behavior score is based on the gaps of the request.

These gaps are either calculated from the requests themselves or they are taken
from the session in the database (if stored there).
